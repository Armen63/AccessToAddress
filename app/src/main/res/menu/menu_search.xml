<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/search"
        android:icon="@drawable/ic_search"
        android:title="Search"
        app:showAsAction="collapseActionView|ifRoom"
        app:actionViewClass="android.support.v7.widget.SearchView"/>

    <group android:id="@+id/menu_top">
        <group android:id="@+id/grp1">
            <item
                android:id="@+id/menu_sort_by_name"
                android:title="By Name" />
            <item
                android:id="@+id/menu_sort_by_name_rev"
                android:title="By Name(rev)" />
            <item
                android:id="@+id/menu_sort_by_access"
                android:title="Access" />
            <item
                android:id="@+id/menu_sort_by_time"
                android:title="Response Time" />
        </group>

    </group>

</menu>
